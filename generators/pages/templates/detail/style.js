import styled from 'styled-components';


export const <%=componentName.substring(0,1).toUpperCase()+componentName.substring(1).toLowerCase()%>Wrapper=styled.div``;
export const Header=styled.div``;
export const Content=styled.div``;